#include<stdio.h>
int main()
{
int x=10;
int y=20;
    //If-else Statement
    if(x>y)
{
printf("%d is greater than %d",x,y);
}
else
{
printf("%d is not greater than %d",x,y);
}
return 0;
}
